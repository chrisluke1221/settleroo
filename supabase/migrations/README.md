These files were reconstructed from the exact sequence of SQL run against
the live project (no local Docker available for `supabase db pull`'s
shadow-database diffing). They reflect what's actually applied, in order.

From here on, schema changes should be written as new timestamped files in
this folder and applied via the SQL Editor (or `supabase db push` once
Docker is available locally), not run ad hoc and discarded.
